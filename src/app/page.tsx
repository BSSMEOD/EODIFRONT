'use client';
import styled from '@emotion/styled';

const MainPage = () => {
  return <StyledMainPage></StyledMainPage>;
};

export default MainPage;

const StyledMainPage = styled.div`
  padding: 40px;
  max-width: 800px;
  margin: 0 auto;
`;
